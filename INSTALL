To install you need to:

1) Execute the git clone commands below in 'etc' folder:

git clone https://github.com/LeaVerou/awesomplete.git
git clone https://github.com/google/recaptcha.git
git clone https://github.com/vlucas/phpdotenv.git

2) Create the file named '.env' in archives-world map root dir and put the lines below. Edit the vars with the values of your preferences:

##
## ARCHIVES WORLD MAP CONFIG
##
AWM_PRIVATE_KEY_RECAPCHA='PRIVATE KEY FROM GOOGLE RECAPTCHA'
AWM_DATABASE_PATH='/var/www/ArchivesMap.db'
AWM_HTTPS_URL='https://map.arquivista.net'
AWM_LOG_PATH='/var/www/archivesworldmap.log'
AWM_BCRYPT_SALT='BCRYPT SALT WITH 22 CHARACTERS'
